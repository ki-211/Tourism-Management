<template><view class="card activity" @click="$emit('open',activity.id)"><image v-if="activity.coverUrl" class="cover" :src="activity.coverUrl" mode="aspectFill" /><view class="row-between"><text class="title">{{ activity.title }}</text><text class="tag">{{ activity.visibility==='PUBLIC'?'公开':'邀请制' }}</text></view><text class="muted">📍 {{ activity.location }}</text><text class="muted">🕒 {{ displayTime(activity.startTime) }} - {{ displayTime(activity.endTime) }}</text><text class="muted">报名至 {{ displayTime(activity.signupEnd) }}</text></view></template>
<script setup lang="ts">import type{Activity}from'@/services/types';import{displayTime}from'@/utils/time';defineProps<{activity:Activity}>();defineEmits<{open:[id:number]}>()</script>
<style scoped lang="scss">.activity{display:flex;flex-direction:column;gap:12rpx}.cover{width:100%;height:260rpx;border-radius:14rpx;margin-bottom:10rpx}.title{font-size:32rpx;font-weight:700}</style>
