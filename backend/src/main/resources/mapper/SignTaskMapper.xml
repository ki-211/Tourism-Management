<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zkt.backend.mapper.SignTaskMapper">

    <select id="selectUnSignedTasksByUserId" resultType="com.zkt.backend.entity.SignTask">
        SELECT st.*
        FROM sign_task st
                 JOIN signup su ON st.activity_id = su.activity_id
        WHERE su.user_id = #{userId}
          AND NOT EXISTS (
            SELECT 1
            FROM sign_record sr
            WHERE sr.task_id = st.id
              AND sr.user_id = #{userId}
        )
    </select>

    <insert id="insert" parameterType="com.zkt.backend.entity.SignTask">
        INSERT INTO sign_task (activity_id, title, description, create_user_id, create_time)
        VALUES (#{activityId}, #{title}, #{description}, #{createUserId}, NOW())
    </insert>

    <select id="selectByActivityId" parameterType="long" resultType="com.zkt.backend.entity.SignTask">
        SELECT * FROM sign_task WHERE activity_id = #{activityId} ORDER BY create_time DESC
    </select>
</mapper>
